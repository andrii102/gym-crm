services:
  postgres:
    image: postgres:17
    container_name: gymcrm-postgres-dev
    restart: always
    environment:
      POSTGRES_DB: gym-app-dev
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    ports:
      - "5433:5432"
    volumes:
      - gymcrm_postgres_data:/var/lib/postgresql/data
    networks:
      - gymcrm-network

  redis:
    image: redis:8
    container_name: gymcrm-redis
    ports:
      - "6379:6379"
    networks:
      - gymcrm-network

  artemis:
    image: vromero/activemq-artemis
    container_name: artemis
    ports:
      - "61616:61616"
      - "8161:8161"
    environment:
      ARTEMIS_USERNAME: ${ARTEMIS_USERNAME}
      ARTEMIS_PASSWORD: ${ARTEMIS_PASSWORD}
    networks:
      - gymcrm-network

  gym-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: gym-app-server
    container_name: gymcrm-container
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      JWT_SECRET: ${JWT_SECRET}
      ARTEMIS_USERNAME: ${ARTEMIS_USERNAME}
      ARTEMIS_PASSWORD: ${ARTEMIS_PASSWORD}
    networks:
      - gymcrm-network
    depends_on:
      - postgres
      - redis
      - artemis

volumes:
  gymcrm_postgres_data:

networks:
  gymcrm-network:
    external: true